services:
  cdn:
    build:
      context: ./cdn
      dockerfile: Dockerfile
    image: ratterscanner/cdn:latest
    container_name: ratterscanner_cdn
    env_file:
      - .env
    ports:
      - "${HOST_PORT}:${PORT}"
    volumes:
    - "${STORAGE_DIR:-./sample_data}:/storage"